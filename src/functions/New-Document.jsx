function newDocument(str) {
    /** Will read the text provided by the API and write a file. At the moment, this will only be a .txt file, but
     * will expand to include more file types down the road. After the new file is created, it will create a hyperlink
     * on the given panel corresponding to the proper assessment or assignment.
     */

}